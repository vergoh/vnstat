
If the daemon is running then stop it first and remove
any manually installed service files.

Run 'make uninstall' and follow the instructions. This step
will not automatically remove any possible databases that have
been created. Such files will need to be removed manually if no
longer needed.
